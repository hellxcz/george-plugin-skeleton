import $ from 'jquery';
import pluginInfo from './pluginInfo'
import helper from './views/i18n'

george.app.module('plugins.' + pluginInfo.name, {

      define: function(testApp) {

        george.app.addInitializer(function () {

        }

    }
});
